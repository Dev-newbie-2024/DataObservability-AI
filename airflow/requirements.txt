# ── Airflow orchestration-only requirements ──────────────────────────────────
# Reference list matching what the Dockerfile installs.
# This file is NOT used during docker build (pip install is run inline
# in the Dockerfile using the official Airflow constraint URL).
#
# Heavy analytics packages (great-expectations, evidently, prophet, scipy)
# are intentionally excluded — they live only in the ai_agents service.
# ─────────────────────────────────────────────────────────────────────────────
# Shared config/ package dependencies (imported at DAG parse time)
structlog>=24.2.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
tenacity>=8.3.0

# Airflow providers
apache-airflow-providers-snowflake>=5.5.0
apache-airflow-providers-apache-kafka>=1.3.0

# Connectors
snowflake-connector-python>=3.10.0
confluent-kafka>=2.4.0

# dbt
dbt-core
dbt-snowflake

# Data
pandas
pyarrow
